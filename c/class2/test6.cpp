#include <stdio.h>

int main() {
    // 定义变量并初始化
    int x = 15, y = 4;
    
    // 计算x除以y的余数
    int z = x % y;
    
    // 输出表达式x除以y的余数
    printf("表达式x除以y的余数是：%d\n", z);
    
    return 0;
}
